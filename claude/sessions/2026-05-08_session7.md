# Session 7 — 2026-05-08

## Fixes

- **Header**: removed "Preparing for" and "· Next stage: N" labels — now just "Next stage" + dropdown
- **Dropdown**: converted from `<input type="number">` back to `<select>` with explicit `#ffffff` text on `#2a3050` background
- **`initStageSelect()`**: populates options 1–21; uses `st.date > TODAY` (not `>=`) so today's completed stage is not re-targeted; parseInt guard prevents NaN/0 defaulting to wrong stage
- **`setTargetStage()`**: guards against invalid input (NaN, 0, out of range); calls `loadSnapshot()` and `renderSliders()`
- **`updateStageLabel()`**: removed entirely — function and all call sites deleted
- **`renderTable()`**: `r._type` guard (`const rtype = r._type || 'Lead-out'`) prevents TypeError crash when riders load without type assigned
- **`loadSnapshot()` + `/snapshot?stage=N`**: already present from session 6b — verified correct
- **`refreshData()`**: already passes `targetStage` and shows bank + team size — verified correct
