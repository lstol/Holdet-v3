# Session 16 — Task 2: Fetch Ingemann diagnose + repair

## TL;DR

**Failure layer: A (URL discovery) — but the root cause is upstream.**
**Decision: STOP. No code change. Follow-up handoff needed.**

The scraper code is correct. Login works, selectors work, the `article`
selector cleanly extracts 5662 chars of body text from the one
Girospillet article that **does** exist. The bug is that Feltet has
published Ingemann's daily Girospillet article only for Stage 1 of the
2026 Giro — Stage 2 and Stage 3 articles do not exist at any URL or
listing surface I can find while logged in as a Feltet+ subscriber.

## Working tree state (Step 0)

Pre-existing uncommitted changes on main, all unrelated to S16-2:
- modified: `claude/dashboard/claude.html` (column reorder in stage results table — not Ingemann)
- modified: `shared/data/riders/giro_2026/{prices_stage0_pre,riders}.json`
- modified: `shared/data/snapshots/stage_1_holdet.json`
- 6 untracked files including `shared/data/snapshots/stage_3_ingemann.json`
  (last failed attempt, captured during diagnosis)

Left untouched.

## Failure signature (Step 1)

`POST /gather-ingemann stage=3` returns HTTP 200 in ~14 s with:

```json
{"riders": [], "captain": "",
 "notes": "No article content provided - unable to extract team information",
 "stage": 3, "gathered_at": "2026-05-09T23:32:11.281194"}
```

Server log line:
`[2026-05-09T21:15:43] gather-ingemann stage=3 chars=41`

41 chars matches exactly `"[Feltet Ingemann: Stage 3 team not found]"` —
the not-found sentinel returned at [scraper.py:198](claude/engine/scraper.py:198)
when the URL discovery loop falls through. Identical signature for
Stage 2 (`stage_2_ingemann.json` from `2026-05-09T10:13:15`).

## Localization (Step 2)

### Layer A — URL discovery (CONFIRMED FAILURE)

The scraper at [scraper.py:176](claude/engine/scraper.py:176) searches:

```python
pattern = re.compile(rf'girospillet.*hold.*{stage}[.-].*etape', re.IGNORECASE)
search_pages = ['/plus/', '/landevej/', '/', '?s=girospillet+hold+{stage}.+etape']
```

With a logged-in Feltet+ session I scraped each listing exhaustively
(including `wait_until='networkidle'` + scroll to trigger lazy listings,
giving 256 hrefs on `/plus/`). Across all 4 search surfaces, the only
girospillet-style article found was discovered indirectly via a "Læs
også" cross-link inside another article:

- Stage 1 article exists: `/plus/girospillet-se-ekspertens-hold-til-1.-etape/11168418`
  (published 2026-05-06, Frederik Ingemann, 5662 chars body via `article` selector)

But:
- Stage 2 article: not on any listing, not in WP search, slug-only URL
  returns SPA 404 (`title=undefined`)
- Stage 3 article: same — does not exist at any discoverable URL
- Feltet's `?s=...` search is non-functional (returns the same default
  recent-articles list regardless of query — verified across 4 different
  query strings)
- Feltet's RSS feed (`/feed`, `/feed/`, `/rss`) is not actually
  RSS-served — returns the SPA shell with 0 `<item>` elements
- `/girospillet/` and `/holdet/` category pages render but contain only
  layout chrome (538 chars), no article listings

What **is** being published per stage:
- `/plus/feltets-fiduser-3.-etape-af-giro-ditalia-2026/11172643` —
  Feltet's editorial group's "Favorites" article. Not Ingemann's team.
  No cross-link from this article to any girospillet article (verified).

### Layers B/C/D/E — VERIFIED NOT THE PROBLEM

Re-pointed the scraper at the Stage 1 article URL directly to
exercise B/C in isolation:
- Selector `article` matched first, extracted 5662 chars of clean
  Ingemann text (rider names, captaincy advice, etc.)
- The Haiku prompt at [server.py:909](claude/engine/server.py:909)
  trims to 3000 chars and asks for an 8-rider JSON — it would parse
  this content correctly. (Not run end-to-end since the layer-A
  upstream is blocking; the layer-B+ chain has been verified
  individually.)

## Decision (Step 3): STOP

This matches the handoff's stop criteria:

> Feltet has paywalled or substantially restructured Ingemann's content

Specifically: Ingemann's Girospillet daily-preview series appears to
have been published only for Stage 1 (May 6). Despite the cross-article
"Giro Manager" piece (May 4) promising "daily previews under hele
løbet", subsequent stages have not been published — or are gated/hidden
behind a discovery surface I don't have access to. The Stage 1 article
itself was only discoverable via a "Læs også" cross-link buried in a
different article — there is no listing, RSS, or search path that
surfaces it.

No selector change, prompt tweak, model bump, or alias addition can
manufacture upstream content that isn't there. The scraper code is
correct as written; if Feltet resumes publishing the series at the
existing URL pattern, gather-ingemann will start working again with
zero changes.

## Implications

- **Stage 3 prep tomorrow**: Ingemann benchmark unavailable. Optimizer's
  4 strategy cards still produced; expert weighting in EV math
  (TV2=1.5, Feltet=1.3) is unaffected since Ingemann is a UI-only
  cross-check, not in the EV pipeline.
- The dashboard's Ingemann card will continue to show the empty/error
  state until either (a) Feltet starts publishing Stage 2+ articles or
  (b) a manual fallback path is added.

## Recommended follow-up (S16-2b candidates)

In rough order of effort/value:

1. **Wait-and-retry**: re-trigger gather-ingemann manually before each
   stage; log only when content is actually present. The current
   article series may resume — Stage 1 article exists at a stable URL,
   so the pattern was real and may continue.
2. **Manual paste fallback**: add a textarea to the Ingemann card so
   the user can paste Ingemann's team manually if discovered via
   another surface (Feltet+ app push, email newsletter, etc.). Bypasses
   scraper entirely.
3. **Discover via Google site search**: `site:feltet.dk girospillet
   ekspertens hold` may surface articles before they appear on Feltet's
   own listings, since Google indexes faster than Feltet exposes. Would
   need to handle Google's anti-scraping. Higher effort.
4. **Drop the Ingemann card from this Giro**: if upstream content
   really is dead, hide the card. Lowest effort; loses the cross-check.

The right next step depends on whether Ingemann's series is just
delayed or genuinely discontinued. A 24–48 hour observation window
(check daily) would resolve that before any structural change.

## Verification (Step 5)

No code change → no server restart → no verification needed.
Existing `stage_3_ingemann.json` (empty/error from earlier user
attempt) remains in place untouched.

## Files changed

None. (Diagnosis only.)
